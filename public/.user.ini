; TeamDark Binary Vault upload limits when public/ is the document root.
; The UI also uses 256 KiB chunked uploads, so strict hosts remain compatible.
upload_max_filesize = 64M
post_max_size = 72M
max_execution_time = 600
max_input_time = 600
memory_limit = 256M
